@model Item
<!-- Here we are receiving that specific model (object) with the relevant id as seen in View(item)
     The item passed from the Details action method becomes accessible throughout the Razor view using 
     the (at)model variable. In the context of the Razor view, (at)model refers to the instance of the Item that was passed from the controller.
-->
<h3 class="my-5">
    @Model.Name
</h3>

<div class="row gx-5">
    <img alt="@Model.Name" src="@Model.ImageUrl" class="img-fluid col-5" />
    <div class="col-7">
        <h5>@Model.Description</h5>
        <h3 class="pull-right">@Model.Price.ToString("0.00 NOK")</h3>
        <p><a href="/Item/Table">Back to Table View</a></p>
        <p><a href="/Item/Grid">Back to Grid View</a></p>
    </div>
</div>