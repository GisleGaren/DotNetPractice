@using MyShop
@using MyShop.Models
@addTagHelper *, Microsoft.AspNetCore.Mvc.TagHelpers

<!-- Note: It is special Razor view file allows you to define common statements applied to all other views in the application.
    It acts as a global import file for your views.
(Line3) is used to add tag helpers, which enables to create custom HTML elements (tags). 
Here it enables routing and navigation, e.g., the pattern of “Controller/Action”.

<a asp-controller="Home" asp-action="Index">Go to Home</a>
Is added in Layout.cshtml because of the addTagHelper 
-->
